// GENERATED CODE - DO NOT MODIFY BY HAND
//
// `post_summary` (a read-only view).
// Generated by `zonai gen client` (zonai 0.8.0).
// Schema: a59bea3e3f7bfa6c3acec0f91380cd284eef8e3fedf0f978111073c6c46090ab
//
// Types say nothing about permission. `canView` / `canList` / `canUpdate` are
// evaluated on the server and still reject at runtime -- a method existing
// here means the shape is known, not that this caller may call it.

// ignore_for_file: type=lint
// dart format off

import 'package:zonai_client/zonai_client.dart';

import '../zonai_runtime.g.dart';

const _r = ZonaiRowReader('post_summary');

/// A `post_summary` row id.
///
/// An extension type over `String`, so it erases at runtime:
/// it survives `jsonEncode` and `SendPort.send` untouched and
/// needs no custom encoder. Passing a different table's id
/// where this one belongs stops compiling.
extension type const PostSummaryId(String value) {
  /// Parses an id from a raw JSON value.
  static PostSummaryId fromJson(Object? json) => switch (json) {
        final String value => PostSummaryId(value),
        _ => throw ZonaiRowParseException(
            table: 'post_summary',
            column: 'id',
            expected: 'id (String)',
            actual: json.runtimeType,
          ),
      };

  String toJson() => value;
}

/// One `post_summary` row, decoded.
///
/// Values arrive as raw SQLite storage and are decoded here:
/// epoch milliseconds become `DateTime`, `0`/`1` becomes
/// `bool`, JSON-encoded text becomes a list or a map.
final class PostSummaryRow {
  const PostSummaryRow({
    required this.id,
    required this.title,
    required this.authorName,
  });

  /// `id` -- id.
  final PostSummaryId id;

  /// `title` -- text.
  final String title;

  /// `author_name` -- text.
  final String authorName;

  factory PostSummaryRow.fromJson(Map<String, Object?> json) => PostSummaryRow(
        id: PostSummaryId(_r.string(json, 'id', kind: 'id')),
        title: _r.string(json, 'title', kind: 'text'),
        authorName: _r.string(json, 'author_name', kind: 'text'),
      );
}

/// Reads of the `post_summary` table.
///
/// A read-only view: it has no `sqlite_master` row and no
/// write operations, so there is nothing else to offer.
///
/// Every method takes an optional `as` and falls through to
/// the client's ambient token when it is omitted.
final class PostSummaryApi {
  const PostSummaryApi(this._db);

  /// The wire name of this table.
  static const table = 'post_summary';

  final Db _db;

  /// The single row with this id.
  Future<PostSummaryRow> get(
    PostSummaryId id, {
    List<String> expand = const [],
    Authorization? as,
  }) =>
      _db.get(
        body: GetBody(
          table: table,
          where: Eq('id', id.value),
          expand: expand,
        ),
        fromJson: PostSummaryRow.fromJson,
        authorization: as?.header,
      );

  /// A page of rows.
  ///
  /// `post_summary` has no foreign keys, so there is
  /// nothing for `expand` to pull in here.
  Future<Paginated<PostSummaryRow>> list({
    Where? where,
    int? limit,
    int? offset,
    List<OrderByTerm>? orderBy,
    List<String> expand = const [],
    Authorization? as,
  }) =>
      _db.list(
        body: ListBody(
          table: table,
          where: where,
          limit: limit,
          offset: offset,
          orderBy: orderBy,
          expand: expand,
        ),
        fromJson: PostSummaryRow.fromJson,
        authorization: as?.header,
      );

  /// How many rows match [where].
  Future<int> count({Where? where, Authorization? as}) =>
      _db.count(
        body: CountBody(table: table, where: where),
        authorization: as?.header,
      );
}

// GENERATED CODE - DO NOT MODIFY BY HAND
//
// `cell_edit_fixtures`.
// Generated by `zonai gen client` (zonai 0.8.0).
// Schema: a59bea3e3f7bfa6c3acec0f91380cd284eef8e3fedf0f978111073c6c46090ab
//
// Types say nothing about permission. `canView` / `canList` / `canUpdate` are
// evaluated on the server and still reject at runtime -- a method existing
// here means the shape is known, not that this caller may call it.

// ignore_for_file: type=lint
// dart format off

import 'package:zonai_client/zonai_client.dart';

import '../zonai_runtime.g.dart';
import 'companies.g.dart';

const _r = ZonaiRowReader('cell_edit_fixtures');

/// A `cell_edit_fixtures` row id.
///
/// An extension type over `String`, so it erases at runtime:
/// it survives `jsonEncode` and `SendPort.send` untouched and
/// needs no custom encoder. Passing a different table's id
/// where this one belongs stops compiling.
extension type const CellEditFixturesId(String value) {
  /// Parses an id from a raw JSON value.
  static CellEditFixturesId fromJson(Object? json) => switch (json) {
        final String value => CellEditFixturesId(value),
        _ => throw ZonaiRowParseException(
            table: 'cell_edit_fixtures',
            column: 'id',
            expected: 'id (String)',
            actual: json.runtimeType,
          ),
      };

  String toJson() => value;
}

/// One `cell_edit_fixtures` row, decoded.
///
/// `secret_note` is a secret column and is stripped from every
/// response, so it has no field here -- absent rather than
/// nullable-and-always-null.
///
/// Values arrive as raw SQLite storage and are decoded here:
/// epoch milliseconds become `DateTime`, `0`/`1` becomes
/// `bool`, JSON-encoded text becomes a list or a map.
final class CellEditFixturesRow {
  const CellEditFixturesRow({
    required this.id,
    required this.label,
    required this.flag,
    required this.count,
    required this.amount,
    required this.bigCount,
    required this.happenedAt,
    required this.contactEmail,
    required this.status,
    required this.tags,
    required this.keywords,
    this.companyId,
    required this.meta,
    this.payload,
    required this.createdAt,
    this.updatedAt,
    this.expanded,
  });

  /// `id` -- id.
  final CellEditFixturesId id;

  /// `label` -- text.
  final String label;

  /// `flag` -- boolean.
  final bool flag;

  /// `count` -- integer.
  final int count;

  /// `amount` -- real.
  final double amount;

  /// `big_count` -- bigInt.
  final BigInt bigCount;

  /// `happened_at` -- dateTime.
  final DateTime happenedAt;

  /// `contact_email` -- email.
  final String contactEmail;

  /// `status` -- enum.
  final String status;

  /// `tags` -- enumList.
  final List<String> tags;

  /// `keywords` -- list.
  final List<Object?> keywords;

  /// `company_id` -- id.
  final CompaniesId? companyId;

  /// `meta` -- map.
  final Map<String, Object?> meta;

  /// `payload` -- blob.
  final List<int>? payload;

  /// `created_at` -- createdAt.
  final DateTime createdAt;

  /// `updated_at` -- updatedAt.
  final DateTime? updatedAt;

  /// Related rows, when the call asked for them. Null
  /// otherwise -- an unexpanded row is not an error.
  final CellEditFixturesExpanded? expanded;

  factory CellEditFixturesRow.fromJson(Map<String, Object?> json) => CellEditFixturesRow(
        id: CellEditFixturesId(_r.string(json, 'id', kind: 'id')),
        label: _r.string(json, 'label', kind: 'text'),
        flag: _r.boolean(json, 'flag', kind: 'boolean'),
        count: _r.integer(json, 'count', kind: 'integer'),
        amount: _r.real(json, 'amount', kind: 'real'),
        bigCount: _r.bigInt(json, 'big_count', kind: 'bigInt'),
        happenedAt: _r.dateTime(json, 'happened_at', kind: 'dateTime'),
        contactEmail: _r.string(json, 'contact_email', kind: 'email'),
        status: _r.string(json, 'status', kind: 'enum'),
        tags: _r.stringList(json, 'tags', kind: 'enumList'),
        keywords: _r.list(json, 'keywords', kind: 'list'),
        companyId: switch (_r.stringOrNull(json, 'company_id', kind: 'id')) { final value? => CompaniesId(value), _ => null },
        meta: _r.map(json, 'meta', kind: 'map'),
        payload: _r.bytesOrNull(json, 'payload', kind: 'blob'),
        createdAt: _r.dateTime(json, 'created_at', kind: 'createdAt'),
        updatedAt: _r.dateTimeOrNull(json, 'updated_at', kind: 'updatedAt'),
        expanded: CellEditFixturesExpanded.tryFromJson(json['expanded']),
      );
}

/// The rows related to a `cell_edit_fixtures` row.
///
/// One field per expandable foreign key, keyed on the wire by
/// the foreign-key column name. Each related row carries its
/// own `expanded`, so depth is recursion rather than a new
/// type.
final class CellEditFixturesExpanded {
  const CellEditFixturesExpanded({
    this.companyId,
  });

  /// The `companies` row `company_id` points at.
  final CompaniesRow? companyId;

  factory CellEditFixturesExpanded.fromJson(Map<String, Object?> json) => CellEditFixturesExpanded(
        companyId: switch (ZonaiRowReader.nested(json['company_id'])) {
          final row? => CompaniesRow.fromJson(row),
          _ => null,
        },
      );

  /// Null when the response carried no `expanded` object.
  static CellEditFixturesExpanded? tryFromJson(Object? json) => switch (ZonaiRowReader.nested(json)) {
        final map? => CellEditFixturesExpanded.fromJson(map),
        _ => null,
      };
}

/// Typed `expand` paths rooted at a `cell_edit_fixtures` row.
///
/// Reach one through `CellEditFixtures.expand`. Each hop is
/// typed by the table it points at, and the value sent is
/// the same dotted string the untyped client takes.
final class CellEditFixturesExpand extends ExpandPath {
  const CellEditFixturesExpand(super.segments);

  /// Expand the `companies` row `company_id` points at.
  CompaniesExpand get companyId => CompaniesExpand([...segments, 'company_id']);
}

/// Typed column tokens for `cell_edit_fixtures`.
///
/// Each token builds a plain `Where` or `OrderByTerm`, so the
/// wire form is exactly what the untyped client sends.
///
/// Columns whose stored value differs from their decoded
/// Dart value get no token -- a photo (stores an id, reads
/// back a URL), and the JSON-encoded kinds (`list`, `map`,
/// `enumList`, blob) plus `bigInt`. A filter built from the
/// decoded value would match nothing or throw, and a token
/// that cannot work is worse than no token at all.
///
/// No token is emitted for `secret_note` --
/// a secret column is stripped from every response and
/// rejected in a filter, so it cannot be named here.
abstract final class CellEditFixtures {
  /// The wire name of this table.
  static const table = 'cell_edit_fixtures';

  /// The root of a typed `expand` path.
  static const expand = CellEditFixturesExpand([]);

  /// The `id` column.
  static const id = ColumnRef<CellEditFixturesId>('id');

  /// The `label` column.
  static const label = ColumnRef<String>('label');

  /// The `flag` column.
  static const flag = ColumnRef<bool>('flag');

  /// The `count` column.
  static const count = ColumnRef<int>('count');

  /// The `amount` column.
  static const amount = ColumnRef<double>('amount');

  /// The `happened_at` column.
  static const happenedAt = ColumnRef<DateTime>('happened_at');

  /// The `contact_email` column.
  static const contactEmail = ColumnRef<String>('contact_email');

  /// The `status` column.
  static const status = ColumnRef<String>('status');

  /// The `company_id` column.
  static const companyId = NullableColumnRef<CompaniesId>('company_id');

  /// The `created_at` column.
  static const createdAt = ColumnRef<DateTime>('created_at');

  /// The `updated_at` column.
  static const updatedAt = NullableColumnRef<DateTime>('updated_at');
}

/// Reads of the `cell_edit_fixtures` table.
///
/// Every method takes an optional `as` and falls through to
/// the client's ambient token when it is omitted.
final class CellEditFixturesApi {
  const CellEditFixturesApi(this._db);

  /// The wire name of this table.
  static const table = 'cell_edit_fixtures';

  final Db _db;

  /// The single row with this id.
  Future<CellEditFixturesRow> get(
    CellEditFixturesId id, {
    List<ExpandPath> expand = const [],
    Authorization? as,
  }) =>
      _db.get(
        body: GetBody(
          table: table,
          where: Eq('id', id.value),
          expand: [for (final e in expand) e.path],
        ),
        fromJson: CellEditFixturesRow.fromJson,
        authorization: as?.header,
      );

  /// A page of rows.
  ///
  /// `groupBy` takes a single column token. The server does
  /// not add aggregates, so rows come back in the ordinary
  /// row shape and `CellEditFixturesRow.fromJson` still applies.
  ///
  /// `expand` takes typed paths -- `[CellEditFixtures.expand.companyId]`.
  /// The server caps depth at 4 and keys each expanded
  /// row by its foreign-key column name.
  Future<Paginated<CellEditFixturesRow>> list({
    Where? where,
    int? limit,
    int? offset,
    List<OrderByTerm>? orderBy,
    ColumnRef<Object?>? groupBy,
    List<ExpandPath> expand = const [],
    Authorization? as,
  }) =>
      _db.list(
        body: ListBody(
          table: table,
          where: where,
          limit: limit,
          offset: offset,
          orderBy: orderBy,
          groupBy: groupBy?.name,
          expand: [for (final e in expand) e.path],
        ),
        fromJson: CellEditFixturesRow.fromJson,
        authorization: as?.header,
      );

  /// How many rows match [where].
  Future<int> count({Where? where, Authorization? as}) =>
      _db.count(
        body: CountBody(table: table, where: where),
        authorization: as?.header,
      );
}

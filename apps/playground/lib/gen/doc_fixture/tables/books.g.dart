// GENERATED CODE - DO NOT MODIFY BY HAND
//
// `books`.
// Generated by `zonai gen client` (zonai 0.8.2).
// Schema: bfe1d2d4804981736d224d03898553a4bcaf59ba61cece12214dea16499fde55
//
// Types say nothing about permission. `canView` / `canList` / `canUpdate` are
// evaluated on the server and still reject at runtime -- a method existing
// here means the shape is known, not that this caller may call it.

// ignore_for_file: type=lint
// dart format off

import 'package:zonai_client/zonai_client.dart';

import '../zonai_runtime.g.dart';
import 'users.g.dart';

const _r = ZonaiRowReader('books');

/// A `books` row id.
///
/// An extension type over `String`, so it erases at runtime:
/// it survives `jsonEncode` and `SendPort.send` untouched and
/// needs no custom encoder. Passing a different table's id
/// where this one belongs stops compiling.
extension type const BooksId(String value) {
  /// Parses an id from a raw JSON value.
  static BooksId fromJson(Object? json) => switch (json) {
        final String value => BooksId(value),
        _ => throw ZonaiRowParseException(
            table: 'books',
            column: 'id',
            expected: 'id (String)',
            actual: json.runtimeType,
          ),
      };

  String toJson() => value;
}

/// The values `books.shelf` declares.
///
/// An extension type over `String`, so it erases at
/// runtime and the wire carries the member name exactly.
/// A member the server adds after this client was
/// generated still arrives and still round-trips --
/// [isKnown] is how you find out it was not declared here.
extension type const BooksShelf(String value) {
  static const wantToRead = BooksShelf('wantToRead');
  static const reading = BooksShelf('reading');
  static const finished = BooksShelf('finished');

  /// Every member the schema declared at generation time.
  static const values = [wantToRead, reading, finished];

  /// False for a value the server added since.
  bool get isKnown => values.contains(this);
}

/// The values `books.tags` declares.
///
/// An extension type over `String`, so it erases at
/// runtime and the wire carries the member name exactly.
/// A member the server adds after this client was
/// generated still arrives and still round-trips --
/// [isKnown] is how you find out it was not declared here.
extension type const BooksTags(String value) {
  static const fiction = BooksTags('fiction');
  static const reference = BooksTags('reference');
  static const borrowed = BooksTags('borrowed');

  /// Every member the schema declared at generation time.
  static const values = [fiction, reference, borrowed];

  /// False for a value the server added since.
  bool get isKnown => values.contains(this);
}

/// One `books` row, decoded.
///
/// Values arrive as raw SQLite storage and are decoded here:
/// epoch milliseconds become `DateTime`, `0`/`1` becomes
/// `bool`, JSON-encoded text becomes a list or a map.
final class BooksRow {
  const BooksRow({
    required this.id,
    required this.title,
    required this.shelf,
    required this.tags,
    required this.ownerId,
    this.expanded,
  });

  /// `id` -- id.
  final BooksId id;

  /// `title` -- text.
  final String title;

  /// `shelf` -- enum.
  final BooksShelf shelf;

  /// `tags` -- enumList.
  final List<BooksTags> tags;

  /// `owner_id` -- id.
  final UsersId ownerId;

  /// Related rows, when the call asked for them. Null
  /// otherwise -- an unexpanded row is not an error.
  final BooksExpanded? expanded;

  factory BooksRow.fromJson(Map<String, Object?> json) => BooksRow(
        id: BooksId(_r.string(json, 'id', kind: 'id')),
        title: _r.string(json, 'title', kind: 'text'),
        shelf: BooksShelf(_r.string(json, 'shelf', kind: 'enum')),
        tags: [for (final v in _r.stringList(json, 'tags', kind: 'enumList')) BooksTags(v)],
        ownerId: UsersId(_r.string(json, 'owner_id', kind: 'id')),
        expanded: BooksExpanded.tryFromJson(json['expanded']),
      );
}

/// The rows related to a `books` row.
///
/// One field per expandable foreign key, keyed on the wire by
/// the foreign-key column name. Each related row carries its
/// own `expanded`, so depth is recursion rather than a new
/// type.
final class BooksExpanded {
  const BooksExpanded({
    this.ownerId,
  });

  /// The `users` row `owner_id` points at.
  final UsersRow? ownerId;

  factory BooksExpanded.fromJson(Map<String, Object?> json) => BooksExpanded(
        ownerId: switch (ZonaiRowReader.nested(json['owner_id'])) {
          final row? => UsersRow.fromJson(row),
          _ => null,
        },
      );

  /// Null when the response carried no `expanded` object.
  static BooksExpanded? tryFromJson(Object? json) => switch (ZonaiRowReader.nested(json)) {
        final map? => BooksExpanded.fromJson(map),
        _ => null,
      };
}

/// Typed `expand` paths rooted at a `books` row.
///
/// Reach one through `Books.expand`. Each hop is
/// typed by the table it points at, and the value sent is
/// the same dotted string the untyped client takes.
final class BooksExpand extends ExpandPath {
  const BooksExpand(super.segments);

  /// Expand the `users` row `owner_id` points at.
  UsersExpand get ownerId => UsersExpand([...segments, 'owner_id']);
}

/// Typed column tokens for `books`.
///
/// Each token builds a plain `Where` or `OrderByTerm`, so the
/// wire form is exactly what the untyped client sends.
///
/// Columns whose stored value differs from their decoded
/// Dart value get no token -- a photo (stores an id, reads
/// back a URL), and the JSON-encoded kinds (`list`, `map`,
/// `enumList`, blob) plus `bigInt`. A filter built from the
/// decoded value would match nothing or throw, and a token
/// that cannot work is worse than no token at all.
abstract final class Books {
  /// The wire name of this table.
  static const table = 'books';

  /// The root of a typed `expand` path.
  static const expand = BooksExpand([]);

  /// The `id` column.
  static const id = ColumnRef<BooksId>('id');

  /// The `title` column.
  static const title = ColumnRef<String>('title');

  /// The `shelf` column.
  static const shelf = ColumnRef<BooksShelf>('shelf');

  /// The `owner_id` column.
  static const ownerId = ColumnRef<UsersId>('owner_id');
}

/// The columns a new `books` row may set.
///
/// Read-only columns are absent: `created_at`, `updated_at`
/// and anything the server generates. A secret column IS
/// here -- it is stripped from responses, not from writes.
final class BooksCreate {
  const BooksCreate({
    this.id,
    required this.title,
    required this.shelf,
    required this.tags,
    required this.ownerId,
  });

  /// The `id` column.
  final BooksId? id;

  /// The `title` column.
  final String title;

  /// The `shelf` column.
  final BooksShelf shelf;

  /// The `tags` column.
  final List<BooksTags> tags;

  /// The `owner_id` column.
  final UsersId ownerId;

  /// The wire object, omitting every column not supplied.
  Map<String, dynamic> toObject() => {
        if (id != null) 'id': zonaiWriteValue(id!.value),
        'title': zonaiWriteValue(title),
        'shelf': zonaiWriteValue(shelf.value),
        'tags': zonaiWriteValue([for (final e in tags) e.value]),
        'owner_id': zonaiWriteValue(ownerId.value),
      };
}

/// The changes to apply to matching `books` rows.
///
/// Each field takes a `Patch` rather than a raw value, which
/// is what makes "leave alone" and "set to NULL" different
/// call sites instead of the same `null`.
final class BooksUpdate {
  const BooksUpdate({
    this.id,
    this.title,
    this.shelf,
    this.tags,
    this.ownerId,
  });

  /// The `id` column.
  final Field<BooksId>? id;

  /// The `title` column.
  final Field<String>? title;

  /// The `shelf` column.
  final Field<BooksShelf>? shelf;

  /// The `tags` column.
  final ListField<BooksTags>? tags;

  /// The `owner_id` column.
  final Field<UsersId>? ownerId;

  /// One `ColumnUpdate` per field actually supplied.
  List<Update> toUpdates() => [
        if (id case final p?) ColumnUpdate('id', p.value),
        if (title case final p?) ColumnUpdate('title', p.value),
        if (shelf case final p?) ColumnUpdate('shelf', p.value),
        if (tags case final p?) ColumnUpdate('tags', p.value),
        if (ownerId case final p?) ColumnUpdate('owner_id', p.value),
      ];
}

/// Reads of the `books` table.
///
/// Every method takes an optional `as` and falls through to
/// the client's ambient token when it is omitted.
final class BooksApi {
  const BooksApi(this._db);

  /// The wire name of this table.
  static const table = 'books';

  final Db _db;

  /// The single row with this id.
  Future<BooksRow> get(
    BooksId id, {
    List<ExpandPath> expand = const [],
    Authorization? as,
  }) =>
      _db.get(
        body: GetBody(
          table: table,
          where: Eq('id', id.value),
          expand: [for (final e in expand) e.path],
        ),
        fromJson: BooksRow.fromJson,
        authorization: as?.header,
      );

  /// A page of rows.
  ///
  /// `groupBy` takes a single column token. The server does
  /// not add aggregates, so rows come back in the ordinary
  /// row shape and `BooksRow.fromJson` still applies.
  ///
  /// `expand` takes typed paths -- `[Books.expand.ownerId]`.
  /// The server caps depth at 4 and keys each expanded
  /// row by its foreign-key column name.
  Future<Paginated<BooksRow>> list({
    Where? where,
    int? limit,
    int? offset,
    List<OrderByTerm>? orderBy,
    ColumnRef<Object?>? groupBy,
    List<ExpandPath> expand = const [],
    Authorization? as,
  }) =>
      _db.list(
        body: ListBody(
          table: table,
          where: where,
          limit: limit,
          offset: offset,
          orderBy: orderBy,
          groupBy: groupBy?.name,
          expand: [for (final e in expand) e.path],
        ),
        fromJson: BooksRow.fromJson,
        authorization: as?.header,
      );

  /// How many rows match [where].
  Future<int> count({Where? where, Authorization? as}) =>
      _db.count(
        body: CountBody(table: table, where: where),
        authorization: as?.header,
      );

  /// Inserts one row and returns it as the server stored it.
  Future<BooksRow> create(
    BooksCreate object, {
    Authorization? as,
  }) =>
      _db.create(
        body: CreateBody(table: table, object: object.toObject()),
        fromJson: BooksRow.fromJson,
        authorization: as?.header,
      );

  /// Inserts many rows in one request.
  Future<List<BooksRow>> createMany(
    List<BooksCreate> objects, {
    Authorization? as,
  }) =>
      _db.createMany(
        body: CreateManyBody(
          table: table,
          objects: [for (final o in objects) o.toObject()],
        ),
        fromJson: BooksRow.fromJson,
        authorization: as?.header,
      );

  /// Applies [set] to the single row matching [where].
  Future<BooksRow> update({
    required Where where,
    required BooksUpdate set,
    Authorization? as,
  }) =>
      _db.update(
        body: UpdateOneBody(
          table: table,
          where: where,
          updates: set.toUpdates(),
        ),
        fromJson: BooksRow.fromJson,
        authorization: as?.header,
      );

  /// Applies [set] to every row matching [where].
  Future<List<BooksRow>> updateMany({
    required Where where,
    required BooksUpdate set,
    int? limit,
    Authorization? as,
  }) =>
      _db.updateMany(
        body: UpdateBody(
          table: table,
          where: where,
          limit: limit,
          updates: set.toUpdates(),
        ),
        fromJson: BooksRow.fromJson,
        authorization: as?.header,
      );

  /// Deletes the single row matching [where].
  Future<void> delete({
    required Where where,
    Authorization? as,
  }) =>
      _db.delete(
        body: DeleteOneBody(table: table, where: where),
        authorization: as?.header,
      );

  /// Deletes every row matching [where].
  Future<void> deleteMany({
    required Where where,
    int? limit,
    Authorization? as,
  }) =>
      _db.deleteMany(
        body: DeleteBody(table: table, where: where, limit: limit),
        authorization: as?.header,
      );

  /// The live-query mirror of `get` / `list` / `count`.
  BooksListen get listen => BooksListen(_db);
}
/// Live queries over `books`.
///
/// Reach one through `client.books.listen`. Each
/// stream is the same subscription `client.db.listen`
/// opens -- this only types the rows coming out of it.
final class BooksListen {
  const BooksListen(this._db);

  /// The wire name of this table.
  static const table = 'books';

  final Db _db;

  /// The single row matching [where], re-emitted on change.
  Stream<BooksRow> one({
    required Where where,
    List<ExpandPath> expand = const [],
    Authorization? as,
  }) =>
      _db.listen.one(
        body: StreamBody(
          table: table,
          where: where,
          expand: [for (final e in expand) e.path],
        ),
        fromJson: BooksRow.fromJson,
        authorization: as?.header,
      );

  /// Every matching row, re-emitted on change.
  ///
  /// A plain `List`, not a `Paginated` -- the streaming
  /// endpoint carries no page metadata.
  Stream<List<BooksRow>> list({
    Where? where,
    int? limit,
    int? offset,
    List<OrderByTerm>? orderBy,
    ColumnRef<Object?>? groupBy,
    List<ExpandPath> expand = const [],
    Authorization? as,
  }) =>
      _db.listen.list(
        body: StreamListBody(
          table: table,
          where: where,
          limit: limit,
          offset: offset,
          orderBy: orderBy,
          groupBy: groupBy?.name,
          expand: [for (final e in expand) e.path],
        ),
        fromJson: BooksRow.fromJson,
        authorization: as?.header,
      );

  /// How many rows match [where], re-emitted on change.
  Stream<int> count({
    required Where where,
    Authorization? as,
  }) =>
      _db.listen.count(
        body: StreamCountBody(table: table, where: where),
        authorization: as?.header,
      );
}


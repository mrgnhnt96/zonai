// A single client-side expression -- `ZonaiStorage.memory()` and the other
// one-liners the storage page shows when the subject is which variant to pick
// rather than how to wire it up.
//
// The list is not decoration: a scaffold has to analyze with an empty body
// too, and `final example = ;` does not parse.
import 'package:zonai_client/server.dart';
import 'package:zonai_client/storage.dart';
import 'package:zonai_client/zonai_client.dart';

final examples = <Object?>[
  // <<body>>
];

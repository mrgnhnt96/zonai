// A list of server-side expressions -- the `Schedule.parse(...)` /
// `RateLimitPolicy(...)` one-liners the reference tables show side by side.
//
// The list is not decoration: a scaffold has to analyze with an empty body
// too, and there is no such thing as a bare expression file.
import 'package:my_app/src/schemas/items.dart';
import 'package:zonai_schema/zonai_schema.dart';

final examples = <Object?>[
  // <<body>>
];

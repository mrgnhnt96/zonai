// A standalone top-level function checked against the real `EmailConfig`
// shape so the port/ssl pairing it validates can't silently drift from the
// fields it actually reads.
import 'package:zonai_schema/zonai_schema.dart';

// <<body>>

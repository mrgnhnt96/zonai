name: zonai_server
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  clock: ^1.1.2
  jaspr: ^0.23.1
  path: ^1.9.1
  revali_client: ^2.0.4
  revali_router: ^3.4.0
  scoped_deps:
    path: ../../libs/scoped_deps
  zonai:
    path: ../zonai
  zonai_web:
    path: ../web

dev_dependencies:
  revali: ^2.1.1
  revali_client_gen: ^2.1.0
  revali_server: ^2.3.0

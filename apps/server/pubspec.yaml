name: zonai_server
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  clock: ^1.1.2
  jaspr: ^0.23.1
  path: ^1.9.1
  revali_client: ^3.0.0
  revali_router: ^5.1.2
  scoped_deps: ^0.2.1
  zonai:
    path: ../zonai
  zonai_web:
    path: ../web

dev_dependencies:
  revali: ^3.3.0
  revali_client_gen: ^2.5.0
  revali_docker: ^1.2.0
  revali_swagger: ^1.3.0
  test: ^1.26.0

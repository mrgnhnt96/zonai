name: zonai_docs
description: Zonai documentation site.
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  jaspr: ^0.23.1
  jaspr_content: ^0.5.2
  jaspr_router: ^0.8.2

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: ^4.8.0
  jaspr_builder: ^0.23.1
  lints: ^5.0.0

jaspr:
  mode: static

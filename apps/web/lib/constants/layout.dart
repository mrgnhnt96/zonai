/// Layout breakpoints and shared panel layout tokens.
abstract final class ZonaiLayout {
  ZonaiLayout._();

  /// Viewport width at or below which slide-over panels use full screen width.
  static const mobilePanelBreakpointPx = 640.0;
}

name: zonai_domain
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  equatable: ^2.0.8
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.10.5
  equatable_gen: ^1.3.0
  json_serializable: ^6.11.2
  test: ^1.26.0

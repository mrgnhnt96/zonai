name: db_operations_usage
description: Minimal stdin/stdout demo for DbOperations (translates operations to SQL).
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  zonai_schema:
    path: ../..
  raindrop:
    path: ../../../raindrop/packages/raindrop
  raindrop_sqlite:
    path: ../../../raindrop/packages/raindrop_sqlite

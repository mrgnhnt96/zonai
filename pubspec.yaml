# ignore_for_file: path_does_not_exist
name: zonai_workspace
publish_to: none

workspace:
  - libs/**
  - support/**
  # Use shallow globs so Revali output under apps/server/.revali/ is not a member.
  - apps/*
  - apps/web/packages/*

environment:
  sdk: ">=3.11.0 <4.0.0"
